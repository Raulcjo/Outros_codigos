import React, { Component } from 'react'
import logo from './logo.svg';
import { render } from '@testing-library/react';
import NovoComponente from './NovoComponente';

methodName = (params) => {
    
  for (const item of object) { //fof
    
  }

  first.forEach(second => {third}) //fre

  setInterval(() => { first }, second) //sti

  setTimeout(() => { first }, second) //sto

  return new Promise((resolve, reject) => { first }) //prom


}

function App() {

  return (
    <div className="App">
      <header className="App-header">
        <img src={logo} className="App-logo" alt="logo" />
        <p>
          Edit <code>src/App.js</code> and save to reload.
        </p>
        <a
          className="App-link"
          href="https://reactjs.org"
          target="_blank"
          rel="noopener noreferrer"
        >
          Learn React
        </a>
      </header>
      <NovoComponente />
    </div>
  );

  render(); {
    return (
      <div>

      </div>
    )
  }; //ren
}

export default App;
